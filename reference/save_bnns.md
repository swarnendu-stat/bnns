# Save a fitted bnns model to disk

Save a fitted bnns model to disk

## Usage

``` r
save_bnns(object, file)
```

## Arguments

- object:

  A fitted `bnns` object.

- file:

  A character string specifying the path where the model should be saved
  (usually ending in .rds).
